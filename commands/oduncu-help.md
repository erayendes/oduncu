---
description: Print the oduncu command list and the current state
---

Print the six oduncu commands and what each does, as a table, in the user's language, followed by one line naming the current state: mode on or off, trace on or off, language. Change nothing — this is not a task, so no `yaparım...` and no `tamam.` Nothing else: no philosophy, no examples.
